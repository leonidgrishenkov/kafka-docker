# Shared KRaft cluster id. Every node must use the same value.
# Regenerate with:
#   docker run --rm --entrypoint /opt/kafka/bin/kafka-storage.sh apache/kafka:4.3.1 random-uuid
# Changing it after the first start requires `docker compose down -v`.
CLUSTER_ID=S7VjXs7ZQVa1jR-UXsCBag
